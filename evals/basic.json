{
  "id": "basic",
  "name": "Basic Eval Set",
  "description": "Minimal examples for verifying eval wiring and file tracking.",
  "version": "1.1.0",
  "cases": [
    {
      "id": "case-file-write",
      "objective": "Create a file at evals/fixtures/basic/hello.txt with a single line: hello eval.",
      "expected": {
        "success": true,
        "contains": ["evals/fixtures/basic/hello.txt"],
        "minScore": 1
      }
    },
    {
      "id": "case-apply-patch",
      "objective": "If evals/fixtures/basic/hello.txt is missing, create it. Then use apply_patch to append a new line: patched.",
      "expected": {
        "success": true,
        "contains": ["evals/fixtures/basic/hello.txt"],
        "minScore": 1
      }
    }
  ]
}
