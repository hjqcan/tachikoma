# Tachikoma 破坏式升级改造计划

> **核心判断**: Tachikoma 不稳定的根本原因不是缺少功能，而是**架构范式错误** —— 它试图用复杂的 Orchestrator+Planner+WorkerPool 多层编排来控制 LLM，而 Claude Code 证明了：**最稳定的 AI agent 架构是一个极简的 flat agentic loop + 极精细的工具/提示词/安全基建**。

## 差距总览

| 维度 | Claude Code | Tachikoma | 差距 |
|------|------------|-----------|------|
| **核心循环** | flat loop: `query() → LLM → tool_use → loop` | Orchestrator → Planner → WorkerPool → Backend → tool | **过度工程化**，4层间接导致状态同步困难 |
| **提示词** | 7层动态组装 + 缓存边界 + 每工具独立 prompt | 简单模板拼接，无缓存优化 | **缺乏系统化提示词工程** |
| **工具定义** | `buildTool()` 工厂 + fail-closed 默认值 | 分散的函数定义，无统一安全属性 | **缺少 fail-closed 设计** |
| **权限系统** | 9层安全审查 + allow/deny/ask/passthrough | 简单的 approval callback | **几乎没有安全体系** |
| **上下文压缩** | 3层压缩(微/自动/完全) + 熔断器 | 2层(compaction + summarization)，无熔断 | **缺少微压缩和熔断器** |
| **子Agent** | fork 模式 + 严格自我意识注入 + Cache 优化 | WorkerPool 多 worker 并行 | **Worker模式不稳定** |
| **记忆系统** | AI 选择记忆 + KAIROS 蒸馏 | 向量检索 MemoryService | **检索精度差** |
| **错误恢复** | 指数退避 + 分类重试 + transcript 持久化 | 简单 catch + 最多1次 replan | **错误恢复能力弱** |

---

## 破坏式改造方案

### Phase 1: 核心架构重构 — 拍扁执行循环

> [!CAUTION]
> 这是最重要也最具破坏性的改造。需要重写 Orchestrator/WorkerPool/Backend 的核心架构。

#### 问题分析

Tachikoma 当前架构:
```
用户消息 → ConversationalRunner → Orchestrator → Planner(LLM调用) → PlanOutput
  → ExecutionLoop → WorkerPool → WorkerExecutor → Backend(LLM调用) → 工具执行
```

Claude Code 架构:
```
用户消息 → QueryEngine.submitMessage() → query() → LLM API → tool_use → 执行工具 → 循环
```

**核心差异**: Claude Code 只有**一个** LLM 调用循环（`query()`），LLM 自己决定用什么工具、以什么顺序执行。Tachikoma 有**两个** LLM 调用（Planner + Worker Backend），中间还有大量状态同步逻辑（SessionFileManager、SharedContext、CheckpointManager 等）。这些中间层是不稳定的主要来源。

#### 改造方案

##### 1.1 新建 `AgenticLoop` 核心引擎

替换 Orchestrator → Planner → ExecutionLoop → WorkerPool → WorkerExecutor 这整条链。

```
新架构:
用户消息 → ConversationalRunner → AgenticLoop → LLM API → tool_use → 工具执行 → 循环
                                     ↑
                             需要并行? → fork 子 Agent (受限制的 AgenticLoop)
```

**关键设计原则** (来自 Claude Code):
- **LLM 是决策者**: 不用 Planner 预生成计划，让 LLM 在每一轮自行决定下一步
- **工具是系统调用**: 工具执行是确定性的，不需要 LLM
- **子 Agent = fork**: 用 fork 模式而非 WorkerPool，子 Agent 共享父的 prompt cache

##### [NEW] `packages/core/src/agentic-loop/loop.ts`

核心执行循环，参考 Claude Code 的 `query.ts`:
- 消息列表管理
- LLM 流式调用
- tool_use 解析和执行
- 并行工具调用支持
- 上下文压缩触发
- 错误重试和熔断

##### [NEW] `packages/core/src/agentic-loop/fork.ts`

子 Agent fork 机制，参考 Claude Code 的 `AgentTool`:
- 继承父的系统提示词（共享 cache）
- 严格的自我意识注入（禁止再次 fork）
- 独立的 AbortController
- 结果收集和上报

##### [DELETE] 以下文件/目录可删除或大幅简化:
- `packages/core/src/orchestrator/engines/` (Planner 驱动的执行不再需要)
- `packages/core/src/orchestrator/worker-pool/` (WorkerPool 替换为 fork)
- `packages/core/src/orchestrator/runner/` (RunService/ExecutionLoop 被 AgenticLoop 替代)
- `packages/core/src/worker/backends/` (多 Backend 抽象过度，简化为单一 LLM 调用层)

---

### Phase 2: 提示词工程系统化

> [!IMPORTANT]
> Claude Code 的提示词是"编译器输出"，不是手写的。这是它"手感好"的核心原因。

#### 2.1 分层提示词组装引擎

##### [NEW] `packages/core/src/prompt/system-prompt-builder.ts`

参考 Claude Code 的 `constants/prompts.ts::getSystemPrompt()`:

```typescript
export async function buildSystemPrompt(config: SystemPromptConfig): Promise<string[]> {
  return [
    // --- 静态内容（可缓存）---
    getIdentitySection(),
    getSystemBehaviorSection(),
    getTaskExecutionSection(),
    getToolUsageSection(config.enabledTools),
    getToneAndStyleSection(),
    getOutputEfficiencySection(),
    
    // === 缓存边界 ===
    SYSTEM_PROMPT_DYNAMIC_BOUNDARY,
    
    // --- 动态内容（每次不同）---
    getEnvironmentSection(config),
    getMemorySection(config.memories),
    getProjectContextSection(config),
    getMCPInstructionsSection(config.mcpClients),
  ].filter(Boolean);
}
```

**关键改进**:
- 静态/动态分离，利用 API prompt cache
- 工具排序固定（cache 稳定性）
- 环境信息自动注入（Git 状态、OS、Shell、cwd）

#### 2.2 每工具独立 Prompt

##### [MODIFY] 所有 `packages/core/src/tools/core/*.ts`

每个工具添加独立的 `prompt` 属性，写给 LLM 看的使用手册:

```typescript
export const shellRunTool: Tool = {
  name: 'shell_run',
  prompt: `## shell_run
  
  Git Safety Protocol:
  - NEVER update git config
  - NEVER run destructive git commands unless explicitly requested
  - ALWAYS create NEW commits rather than amending
  
  Rules:
  - Set working directory via 'cwd' parameter, not 'cd'
  - For long-running processes, use shell_bg instead
  - Check exit code for success/failure`,
  // ...
};
```

---

### Phase 3: 工具系统重构 — fail-closed 设计

> [!IMPORTANT]
> Claude Code 的核心理念: 如果工具作者忘了声明安全属性，系统会假设它是"不安全的、会写入的"。

#### 3.1 `buildTool()` 工厂函数

##### [NEW] `packages/core/src/tools/build-tool.ts`

```typescript
const TOOL_DEFAULTS = {
  isEnabled: () => true,
  isConcurrencySafe: () => false,    // 默认：不安全
  isReadOnly: () => false,           // 默认：会写入  
  isDestructive: () => false,
  maxResultSizeChars: 50_000,
};

export function buildTool<D extends ToolDef>(def: D): BuiltTool<D> {
  return { ...TOOL_DEFAULTS, ...def };
}
```

#### 3.2 先读后改铁律

##### [MODIFY] `packages/core/src/tools/core/file-patch.ts` 和 `file-write.ts`

添加 `validateInput`:
```typescript
validateInput(input, context) {
  if (!context.readFileState.has(input.path)) {
    return {
      result: false,
      message: 'You must read the file before editing. Use file_read first.',
    };
  }
  return { result: true };
},
```

#### 3.3 工具延迟加载 (ToolSearch)

当工具数量 > 20 时，不把所有工具 schema 都注入 prompt。只注入核心工具，其余通过 `tool_search` 按需加载。

##### [NEW] `packages/core/src/tools/tool-search.ts`

---

### Phase 4: 上下文压缩升级 — 三层 + 熔断器

#### 4.1 微压缩层

##### [NEW] `packages/core/src/prompt/strategies/micro-compaction.ts`

只清除旧的工具调用结果内容，保留对话主线:
```typescript
export function microcompact(messages: ContextMessage[]): MicrocompactResult {
  // 替换超过 N 分钟前的 tool_result 内容为 "[Old tool result cleared]"
  // 不触碰用户消息和助手消息
}
```

#### 4.2 压缩熔断器

##### [MODIFY] `packages/core/src/prompt/prompt-engine.ts`

```typescript
private consecutiveCompactionFailures = 0;
private static readonly MAX_COMPACTION_FAILURES = 3;

async autoReduce() {
  // 熔断器：连续失败 3 次后停止尝试
  if (this.consecutiveCompactionFailures >= MAX_COMPACTION_FAILURES) {
    console.warn('Compaction circuit breaker tripped');
    return null;
  }
}
```

#### 4.3 摘要指令强化

在生成摘要时注入严厉的前置指令:
```
CRITICAL: Respond with TEXT ONLY.
Do NOT call any tools.
Tool calls will be REJECTED and will waste your only turn.
```

---

### Phase 5: 权限和安全体系

> [!WARNING]
> 这是 Claude Code 投入最大的领域（18个文件只为 BashTool）。Tachikoma 几乎是空白。

#### 5.1 工具权限决策引擎

##### [NEW] `packages/core/src/permissions/permission-engine.ts`

四态决策: `allow | deny | ask | passthrough`

```typescript
export async function checkPermission(
  tool: Tool,
  input: unknown,
  context: PermissionContext,
): Promise<PermissionResult> {
  // 1. 检查 deny rules（直接拒绝）
  // 2. 检查 allow rules（直接允许）
  // 3. 检查 ask rules（需要用户确认）
  // 4. 默认行为
}
```

#### 5.2 Shell 命令安全

##### [NEW] `packages/core/src/permissions/shell-classifier.ts`

对 shell 命令进行分类:
- **安全**: `ls`, `cat`, `grep`, `find`
- **需确认**: `git push`, `npm publish`  
- **危险**: `rm -rf`, `git push --force`, `git reset --hard`

---

### Phase 6: 记忆系统升级 — AI 选择记忆

#### 6.1 AI 驱动的记忆检索

##### [MODIFY] `packages/core/src/memory/service.ts`

当前使用向量搜索。改为: 用一个小模型（如 Haiku）快速扫描所有记忆文件的标题和描述，选出最相关的 5 个。

策略: **精确度优先于召回率** —— 宁可漏掉可能有用的记忆，也不塞进不相关的记忆。

---

### Phase 7: 错误恢复体系

#### 7.1 分类重试

##### [NEW] `packages/core/src/agentic-loop/retry.ts`

```typescript
export function categorizeError(error: Error): ErrorCategory {
  if (isRateLimitError(error)) return { retryable: true, backoff: 'exponential' };
  if (isNetworkError(error)) return { retryable: true, backoff: 'linear' };
  if (isToolError(error)) return { retryable: false, inform_llm: true };
  if (isContextOverflow(error)) return { retryable: true, action: 'compact_and_retry' };
  return { retryable: false };
}
```

#### 7.2 Transcript 持久化

##### [NEW] `packages/core/src/agentic-loop/transcript.ts`

每一轮对话都持久化到磁盘，即使进程崩溃也能 `--resume` 恢复。

---

### Phase 8: 对话模型简化

#### 8.1 简化 ConversationalRunner

当前的 ConversationalRunner 有 1699 行，承担了太多职责。参考 Claude Code 的 QueryEngine（1296行、职责清晰），将其拆分:

##### [MODIFY] `packages/core/src/conversation/conversational-runner.ts`

- 保留: session 管理 + slash commands
- 移出: Orchestrator 集成 → AgenticLoop
- 移出: checkpoint 逻辑 → 独立的 CheckpointManager
- 移出: MCP 初始化 → 独立的 startup 模块

---

## 执行优先级

```mermaid
gantt
    title Tachikoma 升级改造时间线
    dateFormat X
    axisFormat %s
    
    section Phase 1 - 核心架构
    AgenticLoop 核心循环         :crit, p1a, 0, 5
    Fork 子 Agent 机制          :p1b, after p1a, 3
    删除旧 Orchestrator 层      :p1c, after p1b, 2
    
    section Phase 2 - 提示词
    系统提示词组装引擎          :p2a, after p1a, 3
    每工具独立 Prompt           :p2b, after p2a, 2
    
    section Phase 3 - 工具系统
    buildTool 工厂 + 先读后改    :p3, after p1a, 3
    
    section Phase 4 - 上下文压缩
    微压缩 + 熔断器             :p4, after p1b, 2
    
    section Phase 5 - 权限安全
    权限决策引擎               :p5, after p3, 3
    
    section Phase 6 - 记忆系统
    AI 选择记忆                :p6, after p2a, 2
    
    section Phase 7 - 错误恢复
    分类重试 + Transcript      :p7, after p1a, 2
    
    section Phase 8 - 对话简化
    ConversationalRunner 瘦身   :p8, after p1c, 2
```

## Open Questions

> [!IMPORTANT]
> 1. **LLM Provider 兼容性**: Claude Code 只用 Anthropic API。Tachikoma 支持 OpenAI/Anthropic/Gemini 等多 provider。新的 AgenticLoop 是否需要保留多 provider 支持？还是可以收敛到主要 1-2 个 provider？
>
> 2. **TaskMaster 集成**: 当前通过 TaskMasterAdapter 集成 task-master。在新架构中，任务管理是通过 TodoWrite 工具让 LLM 自行管理（Claude Code 方式），还是保留 TaskMaster 外部编排？
>
> 3. **Session 文件系统**: 当前的 SessionFileManager 管理了大量文件（worker status、shared context、decisions 等）。拍扁架构后，是否保留这套文件系统作为审计日志，还是简化为 Transcript-only？
>
> 4. **破坏性程度**: 这个方案会破坏 ConversationalRunner 的所有下游消费者（CLI、Gateway 等）。是否可以接受同时重构这些消费者？

## Verification Plan

### Automated Tests
1. 核心循环稳定性测试：连续 100 轮对话无崩溃
2. 工具执行正确性测试：所有 38 个工具的单元测试
3. 上下文压缩测试：模拟 200K token 对话的自动压缩
4. 错误恢复测试：模拟 API 超时/限流的自动重试
5. 权限系统测试：shell 命令分类的准确性

### Manual Verification
1. 端到端任务完成率测试：10 个典型编程任务
2. 长会话稳定性测试：4小时以上的连续开发会话
3. 子 Agent 并行执行测试：涉及多文件修改的任务
