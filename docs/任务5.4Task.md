# Task 5.4: Worker 执行器核心流程（混合后端架构）

## 目标

基于讨论的混合架构，实现 Worker Backend 抽象层，支持：

- Claude 模型使用 Claude Agent SDK 后端
- 其他模型（OpenAI、Gemini 等）使用自研通用后端

## 任务清单

### 阶段 1: Worker Backend 接口抽象

- [x] 设计
      [IWorkerBackend](file:///Users/hjqcan/Documents/tachikoma/packages/core/src/worker/types.ts#310-350)
      统一接口
- [x] 定义 Worker 消息类型 ([WorkerMessage](file:///Users/hjqcan/Documents/tachikoma/packages/core/src/orchestrator/types.ts#265-281))
- [x] 创建后端工厂函数 ([createWorkerBackend](file:///Users/hjqcan/Documents/tachikoma/packages/core/src/worker/backend-factory.ts#84-149))

### 阶段 2: Claude Agent SDK 后端实现

- [x] 安装 `@anthropic-ai/claude-agent-sdk` 依赖
- [x] 实现
      [ClaudeAgentSDKBackend](file:///Users/hjqcan/Documents/tachikoma/packages/core/src/worker/backends/claude-agent-backend.ts#57-359)
      类
- [x] 集成 MCP 工具转换逻辑
- [x] 实现消息格式转换（SDK → Tachikoma）

### 阶段 3: 通用后端实现

- [x] 实现
      [GenericAgentBackend](file:///Users/hjqcan/Documents/tachikoma/packages/core/src/worker/backends/generic-agent-backend.ts#276-737)
      类
- [x] 集成现有 LLM Client（支持 OpenAI 等）
- [x] 实现工具调用循环（think → act → observe）
- [x] 集成沙盒执行（LocalSandbox/DockerSandbox）

### 阶段 4: Worker 类集成

- [x] 创建 WorkerExecutor 类 (IWorkerBackend 桥接)
- [x] 集成 SessionFileManager（状态/审批日志）
- [ ] 实现 artifacts 和 metrics 收集 (部分完成)

### 阶段 5: 测试与文档

- [x] 编写 Worker Backend 单元测试
- [x] 编写集成测试
- [x] 更新 README 使用示例

### 阶段 6: 风险修复 (用户反馈)

- [x] SDK bridge: convertToolsToMCPServers 标注为占位
- [x] 降级路径: SDK 导入失败时自动降级到 GenericBackend + 警告
- [x] 能力声明: 调整 getCapabilities 反映实际能力
- [x] 审批策略: 高危检测改为可配置 (RiskPolicy)
- [x] 资源控制: 添加 token 预算和消息窗口限制 (ResourceLimits)

### 阶段 7: 第二轮审查修复

- [x] ResourceLimits 实际执行: SimpleContextManager 使用 maxMessageWindow
- [x] Token 预算追踪: 累积 tokens 并超预算时短路
- [x] maxTokensPerCall 执行: Math.min(config, limits) 限制单次调用
- [x] Token metrics 传递: status 消息携带 tokensUsed
- [ ] Sandbox 集成: 需要 Tool 接口重构 (已添加限制文档)
- [x] MCP 能力移除: 从 getCapabilities 移除未实现的 mcp-tools

---

## 后续优化任务 (Task 5.5+)

### Sandbox 隔离执行 (Medium Priority)

- [ ] 设计 Sandbox.runTool() 接口
- [ ] 在 GenericAgentBackend.executeTool() 中路由到 sandbox
- [ ] 支持 workDir/env 通过 sandbox 传递
- [ ] 无 sandbox 时发出警告或拒绝高危操作

### MCP Tool Bridge (Low Priority)

- [ ] 设计 Tachikoma Tool → MCP Server 适配层
- [ ] 实现 stdio MCP server wrapper
- [ ] 集成到 ClaudeAgentSDKBackend

### 完整 Audit Trail (Low Priority)

- [ ] 扩展 SessionFileManager: appendThinking(workerId, record)
- [ ] 扩展 SessionFileManager: appendAction(workerId, record)
- [ ] 扩展 SessionFileManager: writePendingApproval(workerId, file)
- [ ] WorkerExecutor 调用这些方法持久化事件
